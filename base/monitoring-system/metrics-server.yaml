apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: metrics-server
  namespace: monitoring-system
spec:
  releaseName: metrics-server
  chart:
    git: https://github.com/kubernetes-incubator/metrics-server
    ref: master
  values:
    args:
      - --kubelet-preferred-address-types=InternalIP
